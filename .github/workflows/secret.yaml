name: Show Secret Example

on:
  workflow_dispatch:

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Without backslash — expands to real value
        run: echo "The secret is ${{ secrets.SECRET }}"

      - name: With backslash — stays literal
        run: echo "The secret is \${{ secrets.SECRET }}"

